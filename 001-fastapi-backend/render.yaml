services:
  - type: web
    name: fastapi-backend
    runtime: python
    buildCommand: pip install -r 001-fastapi-backend/requirements.txt
    startCommand: uvicorn 001-fastapi-backend.app.main:app --host 0.0.0.0 --port 10000